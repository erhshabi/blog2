<h1>All categories</h1>
<ul>
    <% @categories.each do |category|%>
        <li>
            <%= link_to category.name, category_path(category), class:"btn btn-lg btn-primary" %>
            <%= category.articles.count %>
        </li>
    <% end %>
</ul>